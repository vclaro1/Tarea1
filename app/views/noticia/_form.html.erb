<%= form_for(@noticium) do |f| %>
  <% if @noticium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noticium.errors.count, "error") %> prohibited this noticium from being saved:</h2>

      <ul>
      <% @noticium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo , class: "form-control"%>
  </div>
  <div class="field">
    <%= f.label :bajada %><br>
    <%= f.text_area :bajada ,class: "form-control", rows: 10%>
  </div>
  <div class="field">
    <%= f.label :cuerpo %><br>
    <%= f.text_area :cuerpo , class: "form-control", rows: 10%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
